<?xml version="1.0" encoding="utf-8"?>
<!-- Nenhuma fonte: o restore não pode sair para a rede. Se algum dia este
     projeto precisar de um pacote, a falha aqui é explícita em vez de virar
     um download silencioso dentro do build da imagem candidata. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
