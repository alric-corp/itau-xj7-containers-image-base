module runtimecontract

// Versão mínima da linguagem, não do toolchain: qualquer Go >= 1.21 compila
// este módulo, então o mesmo projeto serve para go1-25 e go1-26 quando a
// variante -dev correspondente existir.
go 1.21
